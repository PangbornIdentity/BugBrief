# Public-safe placeholders only. Never commit real credentials.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional local Jira import settings. Keep real values in local env or Codex settings.
JIRA_BASE_URL=
JIRA_EMAIL=
JIRA_API_TOKEN=

# Data source: demo or supabase. Demo is the default and requires no database.
BUGBRIEF_DATA_SOURCE=demo
